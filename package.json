{
  "name": "nike",
  "version": "1.0.0",
  "description": "Приложение для учёта проблем",
  "main": "src/main/index.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . Nike --platform=win32 --arch=x64 --out=release --overwrite"
  },
  "devDependencies": {
    "electron": "^42.1.0",
    "electron-packager": "17.1.2"
  },
  "dependencies": {
    "browser-image-compression": "^2.0.2",
    "sharp": "^0.35.1",
    "topbar": "^3.0.1"
  }
}
